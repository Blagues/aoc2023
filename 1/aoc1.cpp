//
// Created by kenzo on 01/12/23.
//

#include "../tools.h"
#include <iostream>

int main() {
    using namespace std;
    std::vector<std::string> data = readData(1);

    int totalsum = 0;

    for (auto &i : data) {
        string f = "";
        string s = "";
        // check if string is a number
        for (auto &j : )
    }
    std::cout << totalsum;
}
